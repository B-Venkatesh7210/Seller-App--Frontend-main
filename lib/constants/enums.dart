enum ButtonSize {
  small,
  medium,
  large,
}

enum ButtonType {
  primary,
  secondary,
  outlined,
}

enum IconPosition {
  leading,
  trailing,
}

enum OrderStatus {
  delivered,
  cancelled,
  placed,
  returned,
}
